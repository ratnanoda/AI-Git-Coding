name: Temporary LiquidBounce External ClickGUI Build

on:
  push:
    branches:
      - tmp/liquidbounce-external-clickgui-build-20260810

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout LiquidBounce nextgen
        uses: actions/checkout@v6
        with:
          repository: CCBlueX/LiquidBounce
          ref: nextgen
          submodules: recursive

      - name: Replace in-game ClickGUI with persistent external browser launcher
        shell: bash
        run: |
          cat > src/main/kotlin/net/ccbluex/liquidbounce/features/module/modules/render/ModuleClickGui.kt <<'KOTLIN'
          /*
           * This file is part of LiquidBounce (https://github.com/CCBlueX/LiquidBounce)
           *
           * Copyright (c) 2015 - 2026 CCBlueX
           *
           * LiquidBounce is free software: you can redistribute it and/or modify
           * it under the terms of the GNU General Public License as published by
           * the Free Software Foundation, either version 3 of the License, or
           * (at your option) any later version.
           */
          package net.ccbluex.liquidbounce.features.module.modules.render

          import net.ccbluex.liquidbounce.LiquidBounce
          import net.ccbluex.liquidbounce.config.types.group.ToggleableValueGroup
          import net.ccbluex.liquidbounce.event.EventManager
          import net.ccbluex.liquidbounce.event.events.ClickGuiScaleChangeEvent
          import net.ccbluex.liquidbounce.event.events.ClickGuiValueChangeEvent
          import net.ccbluex.liquidbounce.features.module.ClientModule
          import net.ccbluex.liquidbounce.features.module.ModuleCategories
          import net.ccbluex.liquidbounce.integration.screen.CustomScreenType
          import net.ccbluex.liquidbounce.integration.theme.ThemeManager
          import net.ccbluex.liquidbounce.utils.client.browseUrl
          import net.ccbluex.liquidbounce.utils.client.inGame
          import org.lwjgl.glfw.GLFW

          object ModuleClickGui :
              ClientModule("ClickGUI", ModuleCategories.RENDER, bind = GLFW.GLFW_KEY_RIGHT_SHIFT, disableActivation = true) {

              override val running get() = true

              @Suppress("UnusedPrivateProperty")
              private val scale by float("Scale", 1f, 0.5f..2f).onChanged {
                  EventManager.callEvent(ClickGuiScaleChangeEvent(it))
                  EventManager.callEvent(ClickGuiValueChangeEvent(this))
              }

              @Suppress("UnusedPrivateProperty", "unused")
              private val searchBarAutoFocus by boolean("SearchBarAutoFocus", true).onChanged {
                  EventManager.callEvent(ClickGuiValueChangeEvent(this))
              }

              val isInSearchBar: Boolean
                  get() = false

              object Snapping : ToggleableValueGroup(this, "Snapping", true) {
                  @Suppress("UnusedPrivateProperty", "unused")
                  private val gridSize by int("GridSize", 10, 1..100, "px").onChanged {
                      EventManager.callEvent(ClickGuiValueChangeEvent(ModuleClickGui))
                  }

                  init {
                      inner.find { it.name == "Enabled" }?.onChanged {
                          EventManager.callEvent(ClickGuiValueChangeEvent(ModuleClickGui))
                      }
                  }
              }

              init {
                  tree(Snapping)
              }

              override fun onEnabled() {
                  if (!LiquidBounce.isInitialized || !inGame) {
                      return
                  }

                  // Static mode keeps the external ClickGUI route independent from
                  // Minecraft's virtual-screen lifecycle. Without this, App.svelte
                  // redirects the browser to /none as soon as it sees no in-game screen.
                  val clickGuiUrl = ThemeManager
                      .getScreenLocation(CustomScreenType.CLICK_GUI, markAsStatic = true)
                      .url

                  browseUrl(clickGuiUrl)
                  super.onEnabled()
              }

              fun sync() = Unit
              fun invalidate() = Unit
          }
          KOTLIN

      - name: Detect JDK version
        id: versions
        shell: bash
        run: |
          set -euo pipefail
          JDK_VERSION=$(grep '^jdk[[:space:]]*=' gradle/libs.versions.toml | head -n1 | sed -n 's/^[^=]*=[[:space:]]*"\([^"]*\)".*/\1/p')
          test -n "$JDK_VERSION"
          echo "jdk=$JDK_VERSION" >> "$GITHUB_OUTPUT"

      - name: Setup JDK
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: ${{ steps.versions.outputs.jdk }}

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Prepare theme permissions
        run: sudo chmod -R 777 src-theme

      - name: Build LiquidBounce
        env:
          CI: true
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew build -x test -x detekt

      - name: Collect main JAR
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifact
          JAR=$(find build/libs -maxdepth 1 -type f -regex '.*/liquidbounce-[0-9.]*\.jar' | head -n1)
          test -n "$JAR"
          cp "$JAR" artifact/liquidbounce-external-clickgui-fixed.jar
          sha256sum artifact/liquidbounce-external-clickgui-fixed.jar | tee artifact/SHA256SUMS.txt

      - name: Upload JAR
        uses: actions/upload-artifact@v4
        with:
          name: liquidbounce-external-clickgui-fixed
          path: artifact/
          if-no-files-found: error
          retention-days: 1
